<launch>
  <!-- Livox Mid360 + IMU 模型 -->
  <arg name="model" default="$(find simple_rbt)/model/sensor/simple_mid360/model.sdf"/>

  <arg name="x" default="0.0"/>
  <arg name="y" default="0.0"/>
  <arg name="z" default="0.0"/>
  <arg name="roll" default="0.0"/>
  <arg name="pitch" default="0.0"/>
  <arg name="yaw" default="0.0"/>

  <node name="spawn_simple_mid360" pkg="gazebo_ros" type="spawn_model"
        args="-file $(arg model) -sdf -model simple_mid360 
              -x $(arg x) -y $(arg y) -z $(arg z)
              -R $(arg roll) -P $(arg pitch) -Y $(arg yaw)"
        output="screen" />

</launch>
